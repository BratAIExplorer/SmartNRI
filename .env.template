# SmartNRI — Environment Variables Template
# Copy this file to /data/smartnri/.env on the VPS and fill in the values.
# NEVER commit the real .env file to Git.

# ── LLM Provider ──────────────────────────────────────────────────────
# Set to "openai" or "gemini"
LLM_PROVIDER=openai

# OpenAI (used if LLM_PROVIDER=openai)
OPENAI_API_KEY=

# Google Gemini (used if LLM_PROVIDER=gemini)
GEMINI_API_KEY=

# ── Telegram Alerts ───────────────────────────────────────────────────
# Create a bot via @BotFather on Telegram, then get the chat ID of your channel
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Email Alerts (optional fallback) ─────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL=

# ── Email Digest (SendGrid) ───────────────────────────────────────────
# For the weekly Sunday digest
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@smartnri.com
SENDGRID_FROM_NAME=SmartNRI

# ── Optional: YouTube API ─────────────────────────────────────────────
# Required for Phase 3 YouTube radar feature
YOUTUBE_API_KEY=
